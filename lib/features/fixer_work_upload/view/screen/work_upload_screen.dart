import 'package:flutter/material.dart';

class WorkUploadScreen extends StatelessWidget {
  const WorkUploadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Upload works'),
      ),
    );
  }
}